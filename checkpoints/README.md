Checkpoints of trained models, currently none required

Could finetune the embedder for better results(extract more impactful, recent information, rather than normal discussion)

If getting enough idea scores, could consider training a model to predict the stock price/stock direction. Not nlp related so not that important. We don't want to trade, we want to see if it would be tradable for now.

We have a trained classifier on top of the categories of episodes, but that is less and less usefull in the direction of where the idea is going.